_.forEachRight (array)
goes from right to left through array

array.push(value)
-add right

array.pop()
--remove right

array.shift()
-pops the left most value

array.unshift(value)
add on the left.

_.map(array, function(item));
returns an array of function values.

C = A || B
C is A if A is defined and else B.

_.forOwn (object, function(val, key))
To iterate over the attributes of an object


_.rest(arguments) returns all arguments but the first.


Testing
------
_.forEach(['one', 'two'], function(method) { it(...)

methodes one and two can be called by object['one']...

check calls of a jasmine spy: function.calls.mostRecent().args[0].name


Arrays
_______

a = [1,2,3,1];
a.forEach(function(item){})

copy: a.slice();

copy subarray: a.slice(from, (to));

indexOf: a.indexOf(1) // 0
indexOf: a.indexOf(1,2) // 3 because it starts at index 2 with search
filter: a.filter(function(element){element === 1}) // [1,1]